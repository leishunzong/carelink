<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.caregiver.carelink.mapper.ServicePackageMapper">

    <!-- 关键词全文检索（FULLTEXT ngram），仅上架，可选分类 -->
    <select id="searchByKeyword" resultType="com.caregiver.carelink.entity.ServicePackage">
        SELECT p.*
        FROM service_package p
        WHERE p.status = 1
        AND MATCH(p.name, p.description) AGAINST(#{keyword} IN NATURAL LANGUAGE MODE)
        <if test="category != null">
            AND p.category = #{category}
        </if>
        ORDER BY MATCH(p.name, p.description) AGAINST(#{keyword} IN NATURAL LANGUAGE MODE) DESC, p.sales DESC
    </select>
</mapper>
